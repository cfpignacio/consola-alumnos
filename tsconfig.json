{
  "compilerOptions": {
    "target": "es2020", // Versión de ECMAScript a la que se compilará el código
    "module": "commonjs", // Sistema de módulos a utilizar (en este caso, CommonJS para Node.js)
    "outDir": "dist", // Carpeta de salida para los archivos JavaScript compilados
    "strict": true, // Activar opciones estrictas de TypeScript
    "esModuleInterop": true // Habilitar la interoperabilidad de módulos ES para CommonJS
  },
  "include": [
    "src/**/*.ts" // Patrón que especifica los archivos TypeScript a incluir en la compilación
  ],
  "exclude": [
    "node_modules" // Directorio que se debe excluir de la compilación
  ]
}